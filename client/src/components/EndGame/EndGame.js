import React from 'react';

const EndGame = ({ scores }) => {

    // show score
    // make form where user can save score
    // then it gets on scoreboard

    // console.log('scores in end game:', scores);

    return(
        <div>
            <h1>Game has ended</h1>
        </div>
    );
}


export default EndGame;

